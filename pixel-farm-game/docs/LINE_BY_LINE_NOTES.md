# 阶段五：近似逐行讲解（按代码段映射）

> 说明：为了可读性，这里按“连续行区间”讲解，而不是每一行都单独写一段。

---

## `src/main.js`

- 第 1-2 行：导入配置与主类。
- 第 4-7 行：抓取 DOM 节点。
- 第 9-16 行：类型守卫，防止节点缺失。
- 第 20-30 行：`runAction` 统一流程（执行动作 -> 自动保存 -> 重绘 -> 状态条）。
- 第 35-60 行：键盘映射。
- 第 62-81 行：商店面板按钮映射（事件委托）。

### 你该理解
- 为什么要把“动作后处理”集中在 `runAction`。
- 为什么移动和作物切换默认不自动保存。

---

## `src/game.js`

- 第 5-26 行：构造函数初始化状态。
- 第 28-50 行：初始化背包与地图结构。
- 第 62-72 行：背包通用加减工具（带上限下限）。
- 第 94-149 行：耕地/播种/浇水基础操作。
- 第 151-173 行：批量买种子逻辑。
- 第 175-212 行：收获与批量卖出逻辑。
- 第 214-239 行：下一天结算（作物生长）。
- 第 241-288 行：手动/自动存档和读档。
- 第 299-341 行：canvas 渲染。
- 第 343-358 行：HUD 文本渲染。

### 你该理解
- 为什么生长参数放在 `CROP_TYPES`，而不是写死在 `nextDay()`。
- 为什么 `tile.cropType` 是必要字段。

---

## `src/config.js`

- 第 2-11 行：全局配置（地图、存档 key、批量数量）。
- 第 14-33 行：多作物参数表。
- 第 36-41 行：地块状态枚举。

### 你该理解
- 配置分离的好处：调参不碰核心逻辑。

---

## `index.html`

- 第 15-34 行：商店按钮区（通过 `data-action` 驱动事件）。
- 第 43 行：HUD 挂载点。
- 第 52 行：底部状态条。
- 第 54 行：入口脚本。

---

## `style.css`

- 第 27-33 行：双栏布局。
- 第 51-93 行：商店和按钮样式。
- 第 120-132 行：底部状态条。

